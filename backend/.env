PORT=5000
JWT_SECRET=silverlink_x_jwt_secret_2024_antigravity
JWT_EXPIRES_IN=7d

# Optional: Add your Anthropic API key to enable human-like AI responses
# Without this, the system falls back to the built-in template engine
ANTHROPIC_API_KEY=

# CORS origins (comma-separated for production)
ALLOWED_ORIGINS=http://localhost:3000
